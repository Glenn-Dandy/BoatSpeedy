1.3.0
* Smart auto-pause: only when the current is low AND the boat is still, so
  drifting keeps recording; both thresholds configurable
* Status chip overrides auto-pause
* Notifications: own settings entry, freely selectable values
* Alerts loop until acknowledged
* Merge trips; sharing several creates one GPX per trip
* Battery: short auto-name, type and MAC, rename
* Fixed radar loading in stages, auto-pause hanging after toggling
