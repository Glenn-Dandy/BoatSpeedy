1.3.1
* Fixed missing battery values (voltage/SoC/remaining/temperature) — short BMS basic frames are now parsed leniently
* Fixed GPX import always showing pause 0:00; own exports carry the exact times, foreign GPX derive moving time from recording gaps
* Fixed merging trips counting the gap between them as pause
* Fixed sharing several GPX files (receiving apps could not open them)
* Battery row decluttered so the connect switch fits; rename/remove moved into the expanded card
* GPX export now includes moving time, pause, total, distance and consumption
